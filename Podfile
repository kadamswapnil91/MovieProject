# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftyJson Work' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftyJson Work
	//pod 'SDWebImage', '~> 4.0'
	pod 'Alamofire', '~> 5.2'

  target 'SwiftyJson WorkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftyJson WorkUITests' do
    # Pods for testing
  end

end
